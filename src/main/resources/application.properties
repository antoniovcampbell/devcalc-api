# Application Configuration
app.name=DevCalc API
app.version=1.0.0
app.description=API REST para operações matemáticas simples

# Server Configuration
server.port=7070
server.host=0.0.0.0
server.context-path=/

# Logging Configuration
logging.level=INFO
logging.file=logs/devcalc-api.log

# API Configuration
api.endpoints.enabled=true
api.cors.enabled=true
api.rate.limit=100